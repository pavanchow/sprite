[package]
name = "sprite"
version = "0.1.0"
edition = "2021"
description = "A tiny, embeddable scripting language in Rust"
license = "MIT"

[lib]
name = "sprite"
path = "src/lib.rs"

[[bin]]
name = "sprite"
path = "src/main.rs"

[dependencies]
clap = { version = "4", features = ["derive"] }
